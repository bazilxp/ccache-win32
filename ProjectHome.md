`ccache-win32` is the native win32 version of samba.org `ccache`.

Faster and without install Cygwin/`cygwin1.dll`

Support following Compilers :
  * MinGW
  * vxWorks GCC
  * Cygwin
  * Other gcc cross-compilation variant under win32